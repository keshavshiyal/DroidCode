# Testing Guide

DroidCode unit test suite covers core business logic, filesystem abstractions, command processing, editor state, and settings persistence.

Execute tests using:
```bash
./gradlew testDebugUnitTest
```
